# Haemo-Agent: Next Steps

Here are the next 3 steps for the project:

1.  **Implement Donor Registration CLI**: Create a command-line interface to allow users to add new donors to the system.
2.  **Integrate Persistent Storage**: Replace in-memory or static data with a persistent storage solution (e.g., SQLite database or JSON file).
3.  **Add Unit Tests**: Write comprehensive unit tests for the `eligibility` module and future components using `unittest` or `pytest`.
